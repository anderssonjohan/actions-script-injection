name: CI
on:
  pull_request:
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Check PR title
        run: |
          echo PR title: ${{ github.event.pull_request.title }}
